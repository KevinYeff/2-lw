#include "main.h"
#include <string.h>
/**
 * _strcat - Concatenates two strings.
 *
 * @dest: Destination.
 * @src: Source.
 */

char *_strcat(char *dest, char *src)
{
	char *s3 = strcat(dest, src);

	return (s3);
}
